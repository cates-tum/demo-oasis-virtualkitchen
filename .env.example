# Copy to .env for local runs. .env is gitignored.

# Base URL of the running pseudo-oasis API.
OASIS_URL=http://localhost:8000

# Optional: set to any integer for reproducible formula noise across runs.
# Leave unset for fresh randomness each experiment.
VK_SEED=
